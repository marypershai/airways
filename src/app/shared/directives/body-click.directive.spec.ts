import { BodyClickDirective } from './body-click.directive';

describe('BodyClickDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyClickDirective();
    expect(directive).toBeTruthy();
  });
});
